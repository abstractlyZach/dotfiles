# Basic binds
super + Return
	$TERMINAL
super + d
	dmenu_run
# open file manager
super + r
	$TERMINAL -e $FILE
super + i
	$TERMINAL -e htop
super + shift + w
	$BROWSER

# super + backtick (`)
super + grave
	dmenu_emoji
